# Website v1

The current live Next.js website remains at the project root:

- `src/`
- `public/`
- `package.json`

This folder is only a marker so the current version is clearly separated from the new `Website-v2` exploration.
